var $form = $('form');
var $toDo = $('#to-do');

$form.on('submit'. function () }
	var $ul = $('<ul>');
	$('body').append('<li>' + input_value + '<a href="">done</a></li>')

$(document).on('click', 'a'. function (e) {
	e.preventDefault();
}